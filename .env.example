# Domains for Traefik routing
DOMAIN=example.com  # Replace with your domain

# Namecheap API credentials for DNS Challenge (required by Traefik)
NAMECHEAP_API_USER=your_namecheap_user  # Replace with your Namecheap API username
NAMECHEAP_API_KEY=your_namecheap_api_key  # Replace with your Namecheap API key

# Note: Remember to never commit sensitive data (like real API keys) to public repositories.
# Copy this file to .env and replace the values with your actual credentials before use.